{
  "name": "wisdom-images-worker",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "secrets:setup": "wrangler secret put UNSPLASH_ACCESS_KEY && wrangler secret put WISDOM_API_KEY"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250508.0",
    "typescript": "^6.0.3",
    "wrangler": "^4.14.4"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild",
      "workerd",
      "sharp",
      "spawn-sync"
    ]
  }
}
