---
name: plan-product
description: Establish product mission, roadmap, and tech stack docs in agent-os/product.
---

# /plan-product

Run the Agent OS product planning workflow for this project.

## Cursor Notes

- Follow the source workflow in `@.claude/commands/agent-os/plan-product.md`.
- This project uses `agent-os/` as the Agent OS directory.
- When the source workflow says to use `AskUserQuestion`, ask the user directly in Cursor chat and wait for the response.
- Keep the conversation lightweight and ask one question at a time.

## Output

Create or update files under `agent-os/product/` as directed by the source workflow.
