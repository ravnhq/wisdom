---
name: index-standards
description: Rebuild the Agent OS standards index from files in agent-os/standards.
---

# /index-standards

Run the Agent OS workflow for rebuilding the standards index.

## Cursor Notes

- Follow the source workflow in `@.claude/commands/agent-os/index-standards.md`.
- This project uses `agent-os/` as the Agent OS directory.
- When the source workflow says to use `AskUserQuestion`, ask the user directly in Cursor chat and wait for the response.
- Keep index descriptions short and alphabetize folders and files.

## Output

Update `agent-os/standards/index.yml`.
